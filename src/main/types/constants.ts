export const MAX_BUFFER_SIZE = 5 * 1024 * 1024 // ~5MB per session (raw text)
export const MAX_INPUT_LENGTH = 64 * 1024 // ~64KB per terminal input
export const MAX_GLOBAL_SESSIONS = 20
export const MAX_CONVERSATION_SCAN_LINES = 10
